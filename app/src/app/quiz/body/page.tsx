import QuizBody from "@/components/quiz/QuizBody";

export const metadata = {
  title: "Your rhythm, unfolding — ĒTHA",
  description: "One map that belongs only to you.",
};

export default function QuizBodyPage() {
  return <QuizBody />;
}
